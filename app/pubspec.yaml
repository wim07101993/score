name: score
description: An application to manage and view my scores.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  bloc: ^7.0.0
  core:
    path: ../core
  flutter:
    sdk: flutter
  google_sign_in: ^5.0.5

dev_dependencies:
  build_runner:
  flutter_test:
    sdk: flutter
  freezed:
  lint: ^1.5.3

flutter:
  uses-material-design: true
