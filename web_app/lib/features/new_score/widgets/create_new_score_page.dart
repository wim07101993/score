import 'package:flutter/material.dart';

class CreateNewScorePage extends StatelessWidget {
  const CreateNewScorePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
