import 'package:flutter/material.dart';

class AddArrangementForm extends StatelessWidget {
  const AddArrangementForm({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
