org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# The two flags below are written here by Flutter's own migrator, and it puts
# them back if they are removed. They only mean anything to AGP 9, and this
# project is pinned to AGP 8, so under this build they say nothing at all.
# Why it is pinned: android/settings.gradle.kts.
# This builtInKotlin flag was added automatically by Flutter migrator
android.builtInKotlin=false
# This newDsl flag was added automatically by Flutter migrator
android.newDsl=false
