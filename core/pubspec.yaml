name: core
description: A new Flutter project.
version: 0.0.1

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  freezed_annotation: ^0.14.2 # latest version uses incompatible meta package version
  hive: ^2.0.4
  logger: ^1.0.0

dev_dependencies:
  build_runner:
  flutter_test:
    sdk: flutter
  freezed:
  lint: ^1.5.3
  testing:
    path: ../testing

flutter:
