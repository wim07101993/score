name: core
description: A new Flutter project.
version: 0.0.1

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  freezed_annotation:
  hive:
  logger:

dev_dependencies:
  build_runner:
  flutter_test:
    sdk: flutter
  freezed:
  lint:
  testing:
    path: ../testing

flutter:
