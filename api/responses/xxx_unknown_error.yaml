description: |
  The request failed for a reason the operation does not name itself. The status
  code says what happened; `405` when the method is not allowed on the path, and
  `500` when the server failed or could not reach the identity provider.
content:
  application/problem+json:
    schema:
      $ref: '../schemas/problem_details.yaml'